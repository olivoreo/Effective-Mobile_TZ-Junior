# User Service API

REST API сервис для управления пользователями с авторизацией и системой ролей.

Проект разработан в рамках тестового задания для IT-компании Effective Mobile по вакансии Junior Node.js-разработчик.

---

## 🚀 Стек технологий

- Node.js
- Express
- TypeScript
- MongoDB (Mongoose)
- JWT (jsonwebtoken)
- bcrypt

---

## 📌 Функциональные возможности

- Регистрация пользователей
- Авторизация с использованием JWT
- Ролевая модель (admin / user)
- Получение профиля пользователя
- Получение списка пользователей (только admin)
- Блокировка пользователей
- Защита маршрутов с помощью middleware
- Хеширование паролей

---

## 📁 Структура проекта

```bash
src/
 ├── config/         # Настройки подключения к БД  
 ├── controllers/    # Бизнес-логика  
 ├── middlewares/    # Middleware (auth, roles)  
 ├── models/         # Mongoose модели  
 ├── routes/         # API маршруты  
 ├── utils/          # Вспомогательные функции  
 ├── app.ts          # Express приложение  
 └── server.ts       # Точка входа  
```

---

## ⚙️ Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/olivoreo/Effective-Mobile_TZ-Junior.git
cd user-service  
```

---

### 2. Установка зависимостей

```bash
npm install
```

---

### 3. Настройка переменных окружения

В корне проекта создайте файл .env на основе примера:

```bash
cp .env.example .env
```

И укажите в нём JWT secret:

```bash
JWT_SECRET=your_secret_key
```

---

### 4. Запуск MongoDB

Убедитесь, что MongoDB запущен локально на порту 27017.

Строка подключения:

```bash
mongodb://localhost:27017/users_db
```

---

### 5. Запуск проекта в режиме разработки

```bash
npm run dev
```

---

### 6. Production сборка

```bash
npm run build  
npm start
```

---

## 📡 API Endpoints

### Регистрация пользователя

```bash
POST /api/users/register
```

Body:
```json
{
  "fullName": "Ivan Ivanov",
  "birthDate": "2000-01-01",
  "email": "ivan@mail.com",
  "password": "123456"
}
```

---

### Авторизация

```bash
POST /api/users/login
```

Body:
```json
{
  "email": "ivan@mail.com",
  "password": "123456"
}
```

Ответ:
```json
{
  "token": "JWT_TOKEN"
}
```

---

### Получить пользователя по ID

```bash
GET /api/users/:id
```

Требуется авторизация.

---

### Получить список пользователей (admin)

```bash
GET /api/users
```

Доступно только для роли admin.

---

### Блокировка пользователя

```bash
PATCH /api/users/:id/block
```

Доступно администратору и владельцу аккаунта.

---

## 🔐 Авторизация

Для доступа к защищённым эндпоинтам используется JWT.

В заголовке запроса:

Authorization: Bearer <token>

---

## 🧪 Тестирование

API протестирован с использованием Postman.

Основные сценарии:

- Регистрация пользователя  
- Авторизация  
- Проверка ролей  
- Проверка прав доступа  
- Блокировка пользователей  

---

## 🛡️ Безопасность

- Пароли хранятся в зашифрованном виде (bcrypt)
- JWT используется для авторизации
- Middleware защищают маршруты
- Ограничение доступа по ролям

---

## 📈 Возможные улучшения

- Использование .env для хранения секретов
- Refresh tokens
- Docker
- Swagger документация
- Unit и integration тесты
- PostgreSQL версия

---

## 👨‍💻 Автор

Проект разработан в учебных и демонстрационных целях.

GitHub: https://github.com/olivoreo
